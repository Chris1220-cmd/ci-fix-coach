{
  "name": "ci-fix-coach",
  "version": "1.0.0",
  "description": "AI-powered CI failure diagnosis — get actionable fix suggestions on your PRs",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "node --test tests/**/*.test.js"
  },
  "keywords": [
    "ci",
    "cd",
    "github-actions",
    "ai",
    "diagnosis",
    "devtools"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
